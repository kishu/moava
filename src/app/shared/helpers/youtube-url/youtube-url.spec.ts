/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {YoutubeUrl} from './youtube-url';

describe('YoutubeUrl', () => {
  it('should create an instance', () => {
    expect(new YoutubeUrl()).toBeTruthy();
  });
});
